/* Configuración del frontend de math2braille.

Único punto del frontend donde se declara dónde vive el backend, para no
repartir URLs por todos los módulos. */

const BACKEND_URL = "http://127.0.0.1:8000";